{
  "name": "@attruvi/worker-links",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "types": "wrangler types --include-runtime false"
  },
  "dependencies": {
    "@attruvi/core": "0.1.0"
  }
}
